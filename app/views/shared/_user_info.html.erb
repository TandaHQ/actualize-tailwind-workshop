<%= link_to gravatar_for(current_user, size: 50), current_user %>
<div class="flex">
	<div>
		<h1 class=""><%= current_user.name %></h1>
		<span><%= link_to "View my profile", current_user, class:"text-blue-400 underline" %></span>
	</div>
	<span class="inline-block"><%= pluralize(current_user.microposts.count, "micropost") %></span>
</div>

