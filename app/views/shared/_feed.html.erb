<% if @feed_items.any? %>
  <ol class="microposts bg-gradient-to-r from-yellow-500 to-purple-100 my-4 p-4 rounded-3xl">
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items,
                    params: { controller: :static_pages, action: :home } %>
<% end %>
